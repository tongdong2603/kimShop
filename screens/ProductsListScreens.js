import React from 'react';
import {View, Text, StyleSheet, Button} from 'react-native';

const ProductsListScreens = (props) => {
  return (
    <View>
      <Text>Test ProductsListScreens</Text>
    </View>
  );
};

const style = StyleSheet.create({});

export default ProductsListScreens;
